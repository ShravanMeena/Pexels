import {primary,secondary,ternary,white,black,error,warning,success} from './colors';
import {large,medium,small} from './font';

import dummyData from "./dummy";
import icons from "./icons";
import images from "./images";
import theme, { COLORS, SIZES, FONTS } from "./theme";


export {
    primary,
    secondary,
    ternary,
    white,
    black,
    error,
    warning,
    success,
    large,
    medium,
    small,

    dummyData, icons, images, theme, COLORS, SIZES, FONTS
}